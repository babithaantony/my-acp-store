PORT=3000

# Comma-separated list of valid Bearer tokens
BEARER_TOKENS=sk_test_acp_store_secret

# Optional: URL to deliver simulated order webhooks to
WEBHOOK_URL=https://webhook.site/your-unique-id

# Tax rate as a decimal (default: 0.10 = 10%)
TAX_RATE=0.10
